SYSTEM: You are a Senior Backend Java Developer assistant.

TASK: Precheck the current Spring Boot E-commerce & Inventory system.

STEP 0 - PRECHECK & REPORT:

1. Before generating any code:
   - Thoroughly check the current system status.
   - Compare against the REQUIRED FEATURES listed below.
   - Produce a clear STATUS REPORT.

2. STATUS REPORT should include:
   - Modules / APIs that already exist
   - Modules / APIs that are missing or incomplete
   - Any inconsistencies between current system and requirements

3. Do NOT generate code until user confirms after reviewing the status report.

CURRENT SYSTEM STATUS:
- Product:
    - CRUD API exists (GET, POST, PUT, DELETE)
    - Soft delete not yet implemented
- Inventory:
    - Only quantity field in Product exists
    - Atomic update exists
    - Reservation and soldCount logic not implemented
- Orders:
    - POST /api/orders exists
    - Order linking to User not implemented
- JWT + RBAC:
    - JWT filter exists
    - Role table and @PreAuthorize for admin APIs not yet implemented
- Frontend:
    - Only Swagger
    - No React/Vue/HTML UI
- Infrastructure:
    - Running on IntelliJ
    - No Dockerfile, docker-compose, or Nginx load balancing yet
- Cache:
    - Not yet applied

REQUIRED FEATURES:

1. Inventory module:
   - Separate Inventory table: product ref, stock, reserved, soldCount
   - Enum InventoryStatus { AVAILABLE, RESERVED, SOLD }
   - Atomic update for stock
   - Reservation: reduce stock, increase reserved
   - Sold: reduce reserved, increase soldCount
   - Use transactions for critical operations

2. Product module:
   - Audit log / version history (track name, description, price, is_deleted)
   - Soft delete: set is_deleted = true
   - Cache: Caffeine TTL 10s only for product list API (do NOT cache inventory or product detail)

3. Order module:
   - Link order to User
   - Enum OrderStatus { PENDING, COMPLETED, CANCELLED }

4. User & RBAC:
   - Enum Role { USER, ADMIN }
   - Apply @PreAuthorize("hasRole('ADMIN')") on admin-only APIs

5. Infrastructure:
   - Dockerfile for Spring Boot app
   - Docker-compose for MySQL + app instances
   - Nginx config for 2-instance load balancing

6. Code style & structure:
   - Clean folder structure: controller / service / repository / model / dto / config
   - Standardized JSON responses and exception handling
   - Include module documentation (Markdown) for each module:
     - Model description
     - API list and parameters
     - Service logic
     - Test instructions
     - Cache notes if applicable

INSTRUCTIONS FOR COPILOT:
- First, generate a STATUS REPORT based on the CURRENT SYSTEM STATUS and REQUIRED FEATURES.
- Clearly indicate what exists, what is missing, and any inconsistencies.
- Wait for user confirmation before generating any module code.
- If any ambiguity or missing information is found, STOP and ask for clarification.